import {
} from '../constants/category';

const initialState = {
};

const config = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default config;
